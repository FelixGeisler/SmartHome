paho-mqtt>=2.0

# BME690 environment sensor (temperature, humidity, pressure) over I2C.
# The BME690 needs its own Bosch driver: it shares the BME680 chip id (0x61) but changed the
# pressure compensation, so the BME680 driver reports pressure far too high. Pulls in smbus2.
bme690>=1.0.0

# CO2 sensor (SCD40/SCD41) over Blinka; only needed when ENABLE_CO2=true.
adafruit-circuitpython-scd4x>=1.4.0
adafruit-blinka>=8.0.0
# Blinka's GPIO backend on current Raspberry Pi OS (Bookworm) and the Pi 5.
lgpio>=0.2.2.0
