= 3. Context and Scope

== Business context

[plantuml,context-diagram,svg]
----
@startuml
left to right direction
actor "API client" as client
rectangle "SmartHome Hub" as hub
node "Smart device" as device
node "MQTT broker" as broker
node "Solar inverter" as inverter
node "Streaming pipeline" as pipeline
cloud "Claude API" as claude
client --> hub : REST / JSON
hub --> device : commands + state
hub --> broker : subscribes\n(sensor readings)
hub --> inverter : reads telemetry\n(Modbus)
hub <--> pipeline : readings out,\nhistory back
hub --> claude : assistant chat
@enduml
----

[cols="1,2,2",options="header"]
|===
| Partner | Sends | Receives
| API client | Registrations, commands, prompts | Device views, state, assistant replies, problem responses
| Smart device | State | Commands
| MQTT broker | Sensor readings (published by sensor nodes) | Nothing
| Solar inverter | Sensor readings (polled over Modbus) | Nothing
| Streaming pipeline | Sensor history (from the search index) | Each recorded reading
| Claude API | Assistant replies, tool calls | Prompts, tool results
|===

== Scope

Register, list, read, and command devices. Around that core the hub ingests sensor readings,
streams them out for history, and exposes the same device operations to the AI assistant.
